apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "art.fullname" . }}-test-connection"
  labels:
{{ include "art.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "art.fullname" . }}']
  restartPolicy: Never
