apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "seeder.fullname" . }}-test-connection"
  labels:
{{ include "seeder.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "seeder.fullname" . }}']
  restartPolicy: Never
