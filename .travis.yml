sudo: required

language: csharp
mono: none
dotnet: 2.0.0
dist: trusty

#safelist
branches:
  only:
  - master

#addons:
#  sonarcloud:
#    organization: "beveragerandomizer"
#    token:
#      secure: "$SONAR_TOKEN"

notifications:
  slack: bevrand:JiQ1Phpayqdnsbi2pdYvdqhJ

services:
  - docker

env:
  - DOCKER_COMPOSE_VERSION=1.8.0

before_install:
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin
  - docker-compose --version
  - docker-compose build
  - docker images
  - docker-compose up -d
  - docker ps -a

script:
 # - sonar-scanner
  - cd ./bevrand.testsuite
  - pwd
  - dotnet test

after_success:
  - echo "$TF_VAR_terraformuser_private_key"
  - cd ../deployment
  - pwd
  - sh ./push_images.sh
  - sh ./init_terraform.sh
  - cd ./terraform_scripts
  - pwd
  - sh ../run_terraform.sh
 # - "$REACTFRONTEND_TRIGGER"
 # - "$MONGOAPI_TRIGGER"
 # - "$RANDOMIZERAPI_TRIGGER"
 # - "$AUTHENTICATIONAPI_TRIGGER"
 # - "$PLAYLISTAPI_TRIGGER"
